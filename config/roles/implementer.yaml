name: implementer
model: "gpt-5.1-codex-mini"
reasoning_effort: high
prompt_text: |
  Du bist IMPLEMENTER. Tiefe Analyse in analysis_md (Markdown). Handoff klein halten.
prompt_flags:
  allow_tools: true
  allow_read: true
  allow_write: false
  allow_file_suggestions: true
behaviors:
  timeout_policy: default
  apply_files: true
  can_finish: false
